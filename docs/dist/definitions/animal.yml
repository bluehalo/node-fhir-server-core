Animal:
  type: object
  properties:
    id:
      type: string
      description: Unique id for the element within a resource (for internal references).
    species:
      type: object
      description: Identifies the high level taxonomic categorization of the kind of animal.
      properties:
        text:
          type: string
          description: Plain text representation of the concept
        coding:
          type: array
          description: Codes available at - http://hl7.org/fhir/valueset-animal-species.html
          items:
            $ref: coding.yml#/Coding
    breed:
      type: object
      description: Identifies the high level taxonomic categorization of the kind of animal.
      properties:
        text:
          type: string
          description: Plain text representation of the concept
        coding:
          type: array
          description: Codes available at - http://hl7.org/fhir/ValueSet/animal-breeds
          items:
            $ref: coding.yml#/Coding
    genderStatus:
      type: object
      description: Indicates the current state of the animal's reproductive organs.
      properties:
        text:
          type: string
          description: Plain text representation of the concept
        coding:
          type: array
          description: Codes available at - http://hl7.org/fhir/ValueSet/animal-genderstatus
          items:
            $ref: coding.yml#/Coding